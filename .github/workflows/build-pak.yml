name: Build .pak

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - name: Install repak
        run: cargo install --git https://github.com/trumank/repak --path repak_cli

      - name: Pack mod
        run: repak_cli pack mod LongerTrailers_P.pak -v --mount-point "../../../"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: LongerTrailers_P
          path: LongerTrailers_P.pak
